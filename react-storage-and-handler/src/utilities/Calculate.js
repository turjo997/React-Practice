const add = (first, second) => {
    return first + second;
}

const multiply = (first, second) => {
    return first * second;
}

//export default add; // for single method


export { add, multiply };
